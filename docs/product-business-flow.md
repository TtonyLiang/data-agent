# 问渠 WenQu 当前业务方向、产品流程与数据链路

> 基准日期：2026-09-01
> 文档用途：作为当前产品业务说明和跨模块协作基线，随代码与产品阶段长期维护。

## 1. 文档维护约定

本文只记录三类内容：

1. 当前代码已经实现的能力。
2. 当前产品实际使用流程和明确边界。
3. 已在现有路线图中确认的后续计划。

以下变化发生时应同步更新本文：

- 产品主线、目标客户或交付闭环变化。
- 前端主导航、角色权限或管理员配置顺序变化。
- 智能问数、风险交付、Ontology、查询语义之间的接口变化。
- 管理库核心表、关键关联 ID 或数据权限规则变化。
- 路线图阶段状态或验收标准变化。

更细的技术实现仍以代码、[项目总体设计](./project-design.md)、[风险报告交付路线图](./risk-report-delivery-roadmap.md)和[Ontology 产品路线图](./ontology-product-roadmap.md)为准。

## 2. 当前业务方向

问渠 WenQu 当前定位为：

> **面向财税服务的 AI 报告交付与风险决策平台。Ontology 作为底层业务模型，连接数据、指标、规则、证据、复核、报告和受控动作。**

产品不只回答一个数据问题，而是希望把数据结果继续推进为可交付、可复核、可追溯的业务结果：

```text
数据 / 受控问数
-> 风险事项
-> 证据链
-> 人工复核
-> 不可变报告版本
-> 受控决策动作
-> 决策审计
```

当前以贷款风控合成数据验证技术闭环。贷款规则、阈值和结论不是已经验证的财税、审计、合规或授信规则。

## 3. 功能块如何联动

| 功能块 | 主要职责 | 与其他模块的关系 |
|---|---|---|
| 对话 | 自然语言问数、多轮追问、SQL、结果、分析和报告展示 | 以智能体为运行入口；读取查询语义、Ontology 上下文和授权 Schema；结果可转风险事项 |
| 智能体管理 | 绑定模型、数据源、默认语义领域和推荐问题 | 决定用户进入对话后能用什么模型、查哪些数据、采用哪套业务口径 |
| 模型配置 | 管理大语言模型和向量模型 | 大语言模型用于理解、LogicForm、兜底 SQL、分析脚本和报告；向量模型用于语义资产召回 |
| 数据源 | 连接业务数据库，读取表清单并采集指定 Schema | 为查询执行和 Ontology 只读对象同步提供物理数据；通过智能体和表/列权限控制边界 |
| 查询语义 | 配置概念、指标、规则、关系、物理映射和 LogicForm 模板 | 把业务语言转换为可校验、可确定性编译的查询口径 |
| 本体建模 | 配置对象、属性、关系、状态、动作、发布版本和对象实例 | 提供业务对象和动作上下文；对象可绑定风险事项；动作进入统一决策审计 |
| 风险交付 | 管理风险事项、证据、人工复核、报告版本和审计 | 承接对话结果和 Ontology 对象，把“查询答案”变成“可复核交付记录” |
| 系统参数 / Prompt | 调整召回阈值、候选表数量和节点 Prompt | 影响运行策略和模型行为，但不替代查询语义资产和权限配置 |
| 用户管理 | 管理用户、角色、状态和用户-智能体授权 | 决定普通用户能看到和使用哪些智能体及其领域数据 |

### 3.1 对话与智能体管理

二者不是独立功能。智能体是对话运行时的配置容器，至少关联：

- 大语言模型与向量模型。
- 一个或多个可访问数据源。
- 默认查询语义领域。
- 推荐问题。
- 用户访问授权。

用户在对话页选择智能体后，查询链路只能使用该智能体绑定的数据源、语义资产和权限范围。

### 3.2 查询语义与 Ontology

二者是同一 `semantic_domain` 下的两条互补链路，不互相替代：

- **查询语义负责聚合分析**：指标是什么、维度是什么、映射到哪张表哪个字段、如何 JOIN、如何生成 SQL。
- **Ontology 负责业务对象与动作**：客户、申请、账户、风险快照等对象当前是什么状态，可以执行什么动作，动作会怎样改变状态。

查询时，语义运行时和 Ontology 上下文会一起进入智能体状态：查询语义指导 LogicForm 和 SQL，Ontology 对象、关系和动作证据参与业务理解及 Schema 打分。

### 3.3 风险交付是两条链路的连接层

风险交付把对话和 Ontology 连接成完整业务流程：

- 从对话结果创建风险事项时，系统按 `agent_id + session_id + trace_id` 精确找到问数轮次。
- 自动冻结问题、LogicForm、SQL、结果预览、分析产物等查询证据。
- 用户可选一个 Ontology 对象，额外冻结该对象的状态快照。
- 产品流程上，风险事项应经过指派复核后再进入报告版本。
- Ontology 动作、风险事项、证据、复核和报告版本共同写入决策审计链。

## 4. 管理员配置流程

按照当前数据依赖，建议使用以下顺序。语义领域必须归属一个智能体，因此智能体需要分两次完成绑定。

1. **配置模型**
   - 添加并测试大语言模型。
   - 添加并测试向量模型。
2. **连接数据源**
   - 添加业务数据库连接并测试。
   - 读取远端表清单，只采集需要参与业务的表。
3. **创建智能体基础配置**
   - 先绑定模型和数据源。
   - 默认语义领域可以暂时留空。
4. **配置查询语义**
   - 为该智能体和数据源创建语义领域。
   - 配置概念、指标、规则、关系、字段映射和 LogicForm 模板。
   - 校验、构建语义运行时并同步向量。
5. **回到智能体管理**
   - 将新建语义领域设置为智能体默认领域。
   - 配置推荐问题。
6. **配置 Ontology**
   - 在同一领域定义或导入对象、关系和动作。
   - 配置对象只读同步 SQL。
   - 完成校验并发布 Ontology release。
7. **配置用户权限**
   - 创建或启用用户。
   - 为普通用户分配可访问智能体。
   - 按需要维护智能体的数据源、表级和列级边界。
8. **按需调整运行配置**
   - 修改 Schema 召回阈值、候选表数量或节点 Prompt。
9. **进入业务运行**
   - 组织问数、风险事项、复核、报告版本、受控动作和审计回放。

注意：表级白名单和列级脱敏运行时已经实现，但当前前端还没有完整的维护入口；现阶段需要通过初始化脚本或管理库维护。

## 5. 普通用户产品流程

### 5.1 登录与选择智能体

- 注册用户默认是启用状态的普通用户。
- 新用户没有智能体权限时无法开展问数，需要管理员授权。
- 普通用户只能看到已授权智能体，以及这些智能体对应的领域。

### 5.2 日常问数

1. 选择智能体或使用管理员配置的默认智能体。
2. 用自然语言提出业务问题。
3. 查看系统的语义增强、语义资产、Ontology 命中、候选表字段和查询过程。
4. 查看最终答案、SQL、结果表；明确要求分析、趋势、报告或图表时进入深度分析。
5. 在同一会话中继续追问、修改时间/维度、重新执行或分析上一轮结果。

### 5.3 从问数进入风险交付

1. 在已完成的查询结果上点击“创建风险事项”。
2. 确认风险标题、分类、严重度、指派复核人和可选 Ontology 对象。
3. 系统自动固化查询证据和分析证据，不需要手工复制 SQL 与结果。
4. 进入风险交付工作台补充证据或查看完整证据链。
5. 被指派的其他用户执行复核；普通用户不能复核自己创建的事项。
6. 创建草稿报告及 V1，后续修订必须创建新版本。
7. 管理员完成最终定稿。

### 5.4 Ontology 业务操作

- 普通用户可以查看自己有领域权限的对象、关系、动作和发布版本。
- 普通用户可以触发业务库只读同步，并查看对象实例。
- 只有满足动作 `allowed_roles`、状态、发布版本、参数、前置条件、审批单号和对象版本校验时才能执行动作。
- 普通用户只能查看自己的动作执行记录，管理员可以查看全部记录。

## 6. 智能问数运行流程

当前问数不是简单的“问题直接交给模型生成 SQL”，而是受控的持久任务循环：

```text
加载 checkpoint
-> 判断本轮是新问题、追问、重试还是继续分析
-> 每次只选择一个白名单动作执行
-> 保存 observation 和 checkpoint
-> 完成、等待补充或进入下一动作
```

一次新的数据问题通常按以下链路推进：

1. **意图识别**：区分数据查询、闲聊和元数据问题。
2. **语义增强**：结合当前问题、历史问题和领域 rewrite 规则补全指标、维度、时间及 TopN。
3. **语义与 Ontology 召回**：加载当前智能体的指标、概念、规则、映射和模板，同时加载当前领域中 active 的对象、关系和按角色可见的动作定义，并携带最新 release 信息。
4. **Schema 定位**：只在已绑定数据源、已采集 Schema 和智能体权限范围内，对表、字段和 JOIN 关系打分。
5. **LogicForm 生成**：把问题转换为指标、维度、过滤、时间、排序和限制等结构化意图。
6. **语义校验**：检查 LogicForm 是否引用了合法资产及可用维度。
7. **确定性 SQL 编译**：命中语义层时由代码编译 SQL；语义层不足或编译失败时才进入受限 NL2SQL 兜底。
8. **SQL 安全与权限校验**：限制为单条只读 SELECT，检查表权限，强制结果上限。
9. **业务库执行与脱敏**：执行查询，并按列级策略对结果做隐藏或脱敏。
10. **回答或深度分析**：简单问题直接整理答案；分析类问题继续进入 Python 安全执行、图表和 Markdown 报告。
11. **持久化**：查询过程、LogicForm、SQL、结果、报告写入 `chat_history`；可续跑状态写入 `agent_task_checkpoint`。

### 6.1 系统如何识别相关表和字段

表和字段不是只靠大模型猜测，而是多种信号共同打分：

- 用户原问题和语义增强后的关键词。
- 语义指标、概念、规则和映射中的名称、描述与同义词。
- Ontology 对象、属性、关系和动作命中结果。
- 已采集表名、表注释、字段名、字段注释和外键。
- 领域配置的 recall profile、schema hint 和业务分组。
- 系统参数中的相对分数阈值和最大候选表数。

最终只有通过用户、智能体、数据源、表和列权限筛选后的 Schema 才会进入 LogicForm、SQL 兜底上下文和查询执行。

## 7. 数据如何串联

### 7.1 三类存储

| 存储 | 保存内容 | 写入边界 |
|---|---|---|
| 业务数据库 | 客户、申请、账户、财税凭证等真实业务数据 | 问数和 Ontology 同步只读；当前 Ontology 动作不直接更新业务库 |
| 管理数据库 | 用户、智能体、模型、数据源、Schema、语义资产、Ontology、会话、风险、报告和审计 | 平台配置和业务工作流的主要落库位置 |
| 向量存储 | 语义资产 embedding | 用于语义召回；失败时可降级为关键词召回 |

### 7.2 关键关联标识

| 标识 | 串联范围 |
|---|---|
| `user_id` | 用户身份、会话归属、任务、动作、复核和审计 |
| `agent_id` | 模型、数据源、语义领域、用户权限和问数运行边界 |
| `datasource_id` | 物理数据库、已采集 Schema、表列权限和 SQL 执行 |
| `domain_id` | 查询语义、Ontology、风险事项、报告和审计的共同业务空间 |
| `session_id` | 一段多轮对话 |
| `trace_id` | 一次问数结果与风险证据的精确血缘 |
| `task_id / turn_id` | 持久任务与某次用户轮次 |
| `ontology_release_id` | 风险、证据、报告版本和动作所依据的业务模型版本 |
| `subject_object_id` | 风险事项关联的 Ontology 对象 |

### 7.3 核心数据链路

```text
datasource
-> meta_table / meta_column
-> semantic_mapping / semantic_metric / semantic_relation
-> LogicForm
-> compiled_sql
-> business database result
-> chat_history + trace_id
-> risk_issue
-> risk_evidence + risk_issue_review
-> risk_report + risk_report_version
-> decision_audit_event / decision_audit_head
```

Ontology 对象同步采用两层属性：

```text
展示和动作校验属性 = source_properties + overlay_properties
```

- `source_properties` 来自业务库只读同步。
- `overlay_properties` 来自 Ontology 动作的本地状态变更。
- 同名字段以 overlay 为准，因此刷新业务库不会覆盖已执行动作的本地结果。

## 8. 权限边界

### 8.1 页面与配置权限

| 能力 | 普通用户 | 管理员 |
|---|---:|---:|
| 对话 | 已授权智能体 | 全部智能体 |
| 风险交付 | 可访问领域内使用 | 可访问全部领域并定稿报告 |
| 本体查看和对象同步 | 可以 | 可以 |
| Ontology 定义、导入、校验、发布 | 不可以 | 可以 |
| Ontology 动作执行 | 按 `allowed_roles` | 按 `allowed_roles` |
| 智能体、模型、数据源、查询语义 | 不可以 | 可以 |
| Prompt、系统参数、用户管理 | 不可以 | 可以 |

### 8.2 数据访问边界

数据访问依次经过：

```text
用户是否可访问智能体
-> 智能体是否绑定数据源
-> 表是否在允许范围
-> 列是否允许或需要脱敏
-> SQL 是否为安全只读查询
```

当前精确规则：

- 没有表权限配置时，智能体默认可以访问已绑定数据源中的已采集表。
- 一旦该智能体/数据源存在表权限记录，未列出的表默认拒绝。
- 未配置列权限时默认允许且不脱敏。
- 列脱敏支持 `redact`、`partial` 和 `hash`。
- SQL 执行前会再次检查表权限，结果返回前再次应用列脱敏。

### 8.3 风险与报告权限

- 普通用户只能把自己的对话结果转换为风险事项；管理员可跨用户做技术验收。
- 普通用户不能复核自己创建的风险事项，并且必须是该事项的指派复核人。
- 普通用户可以创建风险事项、补充证据、创建草稿报告和新版本。
- 报告定稿仅管理员可执行；定稿后不能继续创建新版本。

## 9. 当前实现边界

以下能力当前没有实现，文档和演示中不能当作已有能力：

- 对话 ReAct 尚未自动调用 Ontology 动作；动作主要从本体工作台或受控 REST 工具执行。
- Ontology 同步是进入对象实例页、切换类型、翻页或点击刷新时触发，不是 CDC 或后台定时任务。
- Ontology 动作当前只更新管理库对象和 overlay，不直接写回 ERP、催收、短信或业务数据库。
- `requires_approval` 当前表示执行时必须提供审批单号，不是完整审批流。
- 尚未实现动态 ABAC、生产级动作幂等与失败补偿、分布式图事务。
- 表/列权限运行时已存在，但前端尚无完整维护界面。
- 当前列权限主要控制 Schema 可见性和结果脱敏，SQL 执行前只做表级引用检查，尚未实现完整 SQL 字段级拦截。
- 当前报告创建接口尚未强制要求所绑定风险事项已经复核或确认，复核后出报告目前属于产品流程约束。
- 贷款数据和规则只用于技术演示，不能作为真实业务判断依据。

## 10. 已确认的开发计划

这里仅汇总现有路线图，不新增计划。

### 10.1 产品验证主线

| 阶段 | 当前状态 | 目标 |
|---|---|---|
| A. 贷款技术切片 | 技术实现完成 | 用合成案例验证风险、证据、复核、报告版本、动作和审计契约 |
| B. 内部财税流程选择 | 等待业务团队进入 | 只选择一个高频、资料可得、效果可量化的真实内部流程 |
| C. 历史案例影子运行 | 未开始 | 与专家原流程并行，验证证据追溯、结论差异和时间节省 |
| D. 首个付费试点 | 未开始 | 用明确范围和责任边界验证客户是否愿意为交付结果付费 |

近期执行原则：

1. 先用既有贷款技术切片完成合成案例回放和审计验证。
2. 保持通用问数与 Ontology 能力可用，不继续无业务牵引地扩张贷款规则或对象类型。
3. 内部财税负责人进入后，先确认一个真实流程、历史案例、规则来源、复核角色和报告模板。
4. 影子运行达到验收指标后，再决定客户试点范围和报价。

### 10.2 Ontology 工程演进

| 阶段 | 当前状态 | 已确认范围 |
|---|---|---|
| S1 本体原型 | 已完成 | 对象、关系、动作、图谱、实例、发布、执行审计和最小 Agent 工具 |
| S2 发布治理 | 未开始 | 激活版本、diff/回滚、并发发布和变更影响 |
| S3 数据运行时 | 未开始 | 批量同步、身份解析、唯一/基数约束、坏数据报告 |
| S4 生产动作 | 未开始 | 幂等、正式 HITL 审批、外部写回和失败补偿 |
| S5 Agent 集成 | 未开始 | 在现有最小上下文和 REST 工具基础上，完成生产级工具注册及越权阻断 |
| S6 业务试点 | 未开始 | 真实数据、UAT、运行指标、应急和回滚手册 |

## 11. 代码与文档入口

| 主题 | 主要入口 |
|---|---|
| 产品定位与总体设计 | `README_CN.md`、`docs/project-design.md` |
| 产品验证阶段 | `docs/risk-report-delivery-roadmap.md` |
| Ontology 排期 | `docs/ontology-product-roadmap.md` |
| 问数编排 | `app/agent/graph.py`、`app/agent/react.py`、`app/agent/nodes/` |
| 查询语义运行时 | `app/services/semantic_runtime.py`、`frontend/src/views/KnowledgeConfig.vue` |
| 数据权限 | `app/services/permission_service.py`、`app/utils/sql_validator.py` |
| Ontology | `app/api/ontology.py`、`app/services/ontology_service.py`、`frontend/src/views/OntologyWorkbench.vue` |
| 风险交付 | `app/api/risk_workflow.py`、`app/services/risk_workflow_service.py`、`frontend/src/views/RiskDeliveryWorkbench.vue` |
| 数据模型 | `app/db/schema.sql`、`app/db/ontology_schema.py`、`app/db/risk_schema.py` |
